---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: dnsentries.pfsense.rcluff.com
spec:
  group: pfsense.rcluff.com
  names:
    kind: DnsRecord
    listKind: DnsRecordList
    plural: dnsentries
    singular: dnsrecord
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: DnsRecord is the Schema for the dnsentries API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: spec
            properties:
              aliases:
                description: The list of aliases for the DNS entry.
                items:
                  description: spec.alias
                  properties:
                    domain:
                      description: The domain part of the DNS alias.
                      type: string
                    host:
                      description: The host part of the DNS alias.
                      type: string
                  required:
                  - domain
                  - host
                  type: object
                type: array
              domain:
                description: The domain part of the DNS entry.
                type: string
              host:
                description: The host part of the DNS entry.
                type: string
              ips:
                description: The IP address of the DNS entry.
                items:
                  type: string
                type: array
              service:
                description: The name of the load balancer to use for dynamic DNS.
                type: string
            required:
            - domain
            - host
            type: object
          status:
            description: status
            properties:
              ip:
                description: The current IP address of the DNS entry.
                items:
                  type: string
                type: array
              state:
                description: 'Specifies the state of the DnsRecord. Valid values are:
                  - "" (default): the controller has not processed the request yet;
                  - "Ready": the controller has created the DnsRecord; - "Error": the
                  controller encountered an error reconciling the DnsRecord and will
                  not retry.'
                enum:
                - Ready
                - Error
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
